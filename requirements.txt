# ── Core ────────────────────────────────────────
numpy==1.26.4               # Pinned — stable pre-built wheels on all platforms
opencv-python>=4.8          # Webcam frame capture + image utils
Pillow>=10.0                # Image helpers
python-dotenv>=1.0          # Auto-loads .env into os.environ at startup


# ── Audio Capture ────────────────────────────────
sounddevice>=0.4.6          # Non-blocking mic recording (PortAudio backend)
soundfile>=0.12             # WAV read/write
keyboard>=0.13.5            # Push-to-talk key detection

# ── Speech-to-Text ────────────────────────────────
faster-whisper>=1.0.0       # Faster Whisper STT (CTranslate2 backend)

# ── Emotion — Audio ───────────────────────────────
transformers>=4.38          # Wav2Vec2 + BERT pipelines
# torch + torchaudio: install the CUDA 12.4 wheel (not the default CPU wheel)
# pip install torch==2.6.0+cu124 torchaudio==2.6.0+cu124 --index-url https://download.pytorch.org/whl/cu124
torch>=2.1
torchaudio>=2.1

# ── Emotion — Facial ──────────────────────────────
mediapipe>=0.10           # Face detection + landmark-based cropping

# ── Emotion — Text ───────────────────────────────
# Install when implementing emotion/text_emotion.py:
# transformers>=4.38  (already listed above)

# ── LLM ──────────────────────────────────────────
openai>=1.12                # OpenAI backend (gpt-4o-mini, etc.)
google-genai>=1.0           # Gemini backend (gemini-2.0-flash, etc.)
# Ollama backend uses stdlib urllib — no extra package needed

# ── TTS ──────────────────────────────────────────
edge-tts>=7.0               # Microsoft Edge neural TTS via SSML (no key needed)
# ElevenLabs (optional, premium quality):
# elevenlabs>=1.0           # pip install elevenlabs; set ELEVENLABS_API_KEY

# ── Utilities ─────────────────────────────────────
python-dotenv>=1.0          # Load .env for API keys
